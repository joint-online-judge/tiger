from setuptools import setup

setup(
    name="joj_tiger",
    version="0.0.1",
    packages=["joj.tiger"],
    include_package_data=True,
    install_requires=[],
    setup_requires=["pbr"],
    pbr=True,
)
